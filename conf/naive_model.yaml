hydra:
  job:
    chdir: false

registry: ???
run_dir: null

model_uid: null

dataset:
  batch_size: 32
  missing_rate: 0.1
  name: NP

device: auto

model:
  name: "naive_model"
  mode: last_day # otherwise last_week
  num_epoch : 1
  exog_dim: 2
  hidden_dim: 64
  horizon: 24
  input_dim: 3
  lookback: 24
  lr: 0.001
  output_dim: 1
  target_dim: 1
  teacher_forcing: 0.0

seed: 42

mlflow:
  experiment_name: icassp-dev
  run_name: null

# Il faudra implémenter dans le loader la nouvele lookback, voir ca plus tard
#
# uv run runner.py --config-name naive_model registry=naive_model model.mode=last_week model.lookback=168 model.horizon=168 model.name=last_week_naive
#